const {
  PORT,
  DATABASEURL,
  SMTP_MAIL : SMTPMAIL,
  SMTP_PASSWORD : SMTPPASSWORD,
  SECRETKEY,
} = process.env;

module.exports = {
  PORT,
  DATABASEURL,
  SMTPMAIL,
  SMTPPASSWORD,
  SECRETKEY,
};
